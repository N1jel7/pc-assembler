spring:
  liquibase:
    enabled: true
  application:
    name: pc-assembler
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
logging:
  level:
    sql: debug
springdoc:
  swagger-ui:
    document-title: PC Assembler API
## env
minio:
  root:
    user: ${MINIO_ROOT_USER}
    password: ${MINIO_ROOT_PASSWORD}
